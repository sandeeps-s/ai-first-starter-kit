# PROVENANCE — reused components

One line per borrowed component. Update when you bump a pin. In six months this table is
the difference between a five-minute fix and an afternoon of archeology.

| Component | Source | Pinned commit/tag | Why we use it | Last reviewed | Owner |
|---|---|---|---|---|---|
| superpowers | github.com/obra/superpowers | `<tag/sha>` | Workflow engine: spec→plan→inner-TDD→review→debug | `<date>` | `<name>` |
| claude-code-security-review | github.com/anthropics/... (official) | `<tag/sha>` | Broad SAST as a required CI check | `<date>` | `<name>` |
| <MCP server, e.g. Shopify> | `<url>` | `<tag/sha>` | `<connector for ...>` | `<date>` | `<name>` |

## Upgrade log
Record each pin bump: date, component, old → new pin, what changed, who tested it.

- `<YYYY-MM-DD>` — superpowers `<old>` → `<new>` — `<notes>` — `<name>`
